#include <iostream>
#include <vector>
using namespace std;
int main(){
    vector<vector<int>>v ;
    int arr[2][3]  = {1,2,3,4,5,6};
    // v.push_back(10);
    // v.push_back(20);
    // v.push_back(20);
    // v.push_back(30);
    // v.push_back(40);
    int m = v.size();
    int n = v[0].size();
    cout << m << " " << n ;
}